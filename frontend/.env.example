# Only needed when hosting frontend separately from backend.
# If Express serves the built frontend (combined deployment), leave blank.
# Example for Netlify/Vercel pointing to Railway/Render backend:
# VITE_API_URL=https://your-backend.railway.app/api
VITE_API_URL=
